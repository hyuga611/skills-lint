# Security Policy

`skills-lint` runs locally / in CI, has **zero runtime dependencies**, and only reads files you point it at — it makes no network calls and writes nothing.

If you find a security issue, please open a private security advisory on the GitHub repository rather than a public issue.
